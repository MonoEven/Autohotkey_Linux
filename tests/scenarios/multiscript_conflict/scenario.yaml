id: multiscript_conflict
title: "Conflicted X11 hotkey reports BadAccess then auto-recovers after owner exit"
script: run.ahk
check: script:grep -q '^conflict:' /tmp/scn_multiscript_conflict && grep -q '^recovered=1$' /tmp/scn_multiscript_conflict
envs: x11
expect: pass
needs: ""
